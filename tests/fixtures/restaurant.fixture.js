const { Restaurant } = require('../../src/models');
const Restaurant = require('../../src/models/restaurant.model');

const searchOne = {  
  name: "McDonalds",
  rate: 1,
  distance: 1,
  price: 1,
  cuisine: "American"
};

const searchRestaurants = async (restaurants) => {
  
};

module.exports = {
  searchOne
};
